(version 1)

# 自訂設計規則。用規則檔而不是網路類別，是因為規則的「依據」可以直接寫在旁邊 ——
# 安全性間距被誰改掉、為什麼是這個數字，半年後回來看得懂。

# ---------------------------------------------------------------- 高壓
# IPC-2221B 表 6-1，101–150 V DC，外層導體：
#   B4 外層有永久性高分子塗層（＝綠漆）..... 0.4 mm
#   B2 外層裸露、海平面 ..................... 3.2 mm
# 本板全區上綠漆，取 B4 的 0.4 mm，再加 25% 餘裕 → 0.5 mm。
#
# V1.3 實測：走線對走線 0.701 mm（合格），但 GND 鋪銅逼近到 0.249 mm（不合格）。
# 問題只出在鋪銅，所以這條規則生效後重新鋪銅即可，不必重繞。
(rule "HV-120V 對其他網路間距"
	(constraint clearance (min 0.5mm))
	(condition "A.NetName == '120V' && B.NetName != '120V'"))

# GND_BACK 刻意「不」比照 120V 拉開。
# 降壓模組是非隔離 buck（IN- 與 OUT- 共通），所以 GND_BACK 和板子 GND 實質同電位，
# 它對 VOUT_SENSE 的電位差只有 4 V（120V 經 348k/12k 分壓）。
# 之前對 GND_BACK 也套 0.5 mm，結果把 U5 第 4、5 腳判成違規 ——
# 那是 MSOP-10 相鄰腳位，腳距 0.5 mm，本來就不可能拉開。
# 真正決定間距的是「兩條網路之間的電位差」，不是「這條網路有沒有碰過高壓」。

# 高壓走線寬度下限。電流很小（120V 經 348k 分壓，約 0.35 mA），
# 寬度不是為了載流，是為了蝕刻餘裕 —— 細線上的蝕刻缺口在高壓下更容易起弧。
# 0.25 mm（10 mil）是嘉立創標準製程的舒適區，V1.3 用的就是這個值。
(rule "HV 走線最小寬度"
	(constraint track_width (min 0.25mm))
	(condition "A.NetName == '120V'"))

# ---------------------------------------------------------------- 電源
# IPC-2221 載流量：I = 0.048 * dT^0.44 * A^0.725（A 為 mil^2，外層 1oz）
# 12V 幹線要扛兩顆繼電器線圈約 1 A ＋ 電磁鎖 ＋ U8 輸入。
# 外層 1 oz、dT 10°C 下：0.762 mm（30 mil）約 2.0 A，1.27 mm（50 mil）約 2.9 A。
# V1.3 主幹 1.27 mm、分支 0.762 mm，兩者都夠，所以下限取 0.75 mm ——
# 訂 0.8 mm 只會把本來就合格的 30 mil 分支全部判成違規。
(rule "12V 幹線最小寬度"
	(constraint track_width (min 0.75mm))
	(condition "A.NetName == '12V'"))

(rule "繼電器與電磁鎖輸出最小寬度"
	(constraint track_width (min 0.6mm))
	(condition "A.NetName == 'DC_RELAY' || A.NetName == 'COUPLER' || A.NetName == 'VP'"))

# ---------------------------------------------------------------- 例外
# USB-C（TYPE-C16PIN / C393939）的外殼腳銑槽就開在自己的焊盤旁邊，
# 實測焊盤到槽 0.177–0.210 mm、VBUS 走線到槽 0.156 mm。
# 這是這顆連接器的固有幾何，不是佈線失誤 —— 我們沒辦法把它的焊盤移遠一點。
# V1.1／V1.2 用同一個封裝已經在嘉立創量產過，製程接受。
# 只對「USB1 自己的銅箔 vs 自己的槽」放寬，板子其他地方仍是 0.3 mm。
(rule "USB1 外殼腳銑槽的自身間距"
	(constraint edge_clearance (min 0.15mm))
	(condition "A.memberOfFootprint('USB1')"))

# ---------------------------------------------------------------- 組裝
# 決定「這兩顆零件擺得下嗎」的是焊盤之間的實體距離，不是外框餘裕有沒有碰到。
# EasyEDA 轉來的封裝外框margin 給得很寬（0603 給到 0.25mm 以上），
# 兩顆相鄰的零件外框幾乎一定會重疊，但焊盤其實離得很開。
#
# V1.3 現況：20 對外框重疊，焊盤間距 0.356–3.671 mm，最小 0.356 mm。
# 嘉立創 SMT 的零件間距下限是 0.2 mm，而且 V1.1／V1.2 用同樣的擺放量產過。
# 這裡把真正的約束訂在 0.3 mm 並交給 DRC 把關；
# courtyards_overlap 則在專案設定裡降為警告 —— 它仍然會提醒，但不會蓋掉真正的錯誤。
(rule "元件焊盤實體間距"
	(constraint physical_clearance (min 0.3mm))
	(condition "A.Type == 'Pad' && B.Type == 'Pad' && A.Reference != B.Reference"))
